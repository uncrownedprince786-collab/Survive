# Canonical site origin (no trailing slash). Set this to your deployed URL.
NEXT_PUBLIC_SITE_URL=https://survive.vercel.app

# Reserved for a future version. V1 runs fully offline and does NOT read this.
# DATABASE_URL=postgresql://user:password@host/db?sslmode=require
